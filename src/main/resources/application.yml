server :
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smart_energy_schema_ab
    username: root
    password: spring123
    driver-class-name: com.mysql.cj.jdbc.Driver


  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
        show_sql: true

  main:
    web-application-type: servlet
